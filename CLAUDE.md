# FB入金データ生成ツール 仕様書

## 1. プロジェクト概要

### 目的
デモ用の全銀協フォーマット準拠の入金FBデータ（ファイル）を生成するデスクトップGUIツール。

### 成果物
- Windows向け単一実行ファイル（`.exe`）
- 全銀協フォーマット準拠のFBデータファイル（Shift-JIS）

---

## 2. 技術スタック選定

### 選定結果：**Tauri v2 + React (TypeScript)**

| レイヤー | 技術 |
|---|---|
| フロントエンド（GUI） | React + TypeScript + Vite |
| バックエンド（ロジック） | Rust（Tauriコマンド） |
| デスクトップフレームワーク | Tauri v2 |
| スタイリング | Tailwind CSS |

### 選定理由

**Tauriを選んだ理由（vs Python単体）**

- **単一exeへの親和性**：Tauri v2は`tauri build`で単一の`.exe`を生成できる（Pythonは PyInstaller等でバンドルすると起動が遅く、アンチウイルス誤検知リスクがある）
- **Shift-JIS出力**：Rustの`encoding_rs`クレートで確実にShift-JISエンコードできる
- **全銀協フォーマットの固定長バイト処理**：Rustはバイト列操作が得意で、全銀協の固定長レコード（120バイト）を正確に扱える
- **GUI品質**：WebViewベースのUIでリッチなフォームを実装しやすい

**Pythonを採用しなかった理由**

- 単一exe化の信頼性が低い（PyInstallerの起動遅延・AV誤検知）
- Shift-JIS/固定長バイト処理でのエンコーディング事故リスク

---

## 3. 機能要件

### 3-1. GUIで入力する情報（FBヘッダー情報）

| フィールド | 内容 | 備考 |
|---|---|---|
| 入金日 | YYYY/MM/DD形式 | 作成日・勘定日(自)・勘定日(至)・データレコードの勘定日・起算日すべてに同じ値を使用。出力時にYYMMDD（和暦）へ変換 |
| 銀行コード | 4桁数字 | デフォルト`0005`（三菱UFJ）、GUI入力で変更可 |
| 銀行名 | 半角カタカナ15文字以内 | デフォルト`ミツビシユーエフジエイ`、GUI入力で変更可 |
| 支店コード | 3桁数字 | 照会口座の支店番号 |
| 支店名 | 半角カタカナ15文字以内 | 照会口座の支店名（カナ）、左詰めスペース埋め |
| 預金種目 | 1桁（1:普通預金, 2:当座預金） | プルダウン選択 |
| 口座番号 | 7桁数字（右詰め、前は"0"で埋める） | 照会口座の口座番号 |
| 出力先ファイルパス | ファイル保存先 | ダイアログで選択 |

**注意**：口座名（ヘッダー項番13）はスペース40個の固定値として出力する。GUI入力不要。

### 3-2. CSVファイルで入力する情報（入金明細）

**入力CSVフォーマット（ヘッダー行あり、文字コード：Shift-JIS）**

```
金額,銀行名(カナ),支店名(カナ),摘要文字列,EDI
100000,トウキヨウギンコウ,シンジユクシテン,カブシキガイシャサンプル,
```

| フィールド | 型 | 桁数・制約 | FBレコードの対応項目 |
|---|---|---|---|
| 金額 | 整数 | 1以上、12桁以内 | 金額(1)（項番5）10桁以内の場合、金額(2)（項番12）10桁超の場合 |
| 銀行名(カナ) | 文字列 | 半角カタカナ・半角英数字、15文字以内、空欄許容 | 仕向銀行名（項番9） |
| 支店名(カナ) | 文字列 | 半角カタカナ・半角英数字、15文字以内、空欄許容 | 仕向店名（項番10） |
| 摘要文字列 | 文字列 | 半角カタカナ・半角英数字、48文字以内 | 振込依頼人名（項番8） |
| EDI | 文字列 | 20文字以内、空欄許容 | EDI情報（項番14） |

**固定値・自動設定項目**
- 照会番号（項番2）：CSV読み込み時に連番（000001〜）を自動採番
- 勘定日（項番3）・起算日（項番4）：GUI入力の入金日をYYMMDD（和暦）に変換して全明細共通で使用
- 振込依頼人番号（項番7）：固定値`1234567890`
- 他店券金額（項番6・13）：ALLゼロ固定
- 取消区分（項番11）：`0`（振込）固定
- カナコメントリスト（項番16）：スペース埋め固定
- ダミー（項番15・17）：スペース埋め固定

### 3-3. 出力FBデータ仕様（全銀協フォーマット）

**文字コード**：Shift-JIS  
**レコード長**：200バイト固定  
**改行コード**：CRLF（`\r\n`）  
**数字フィールド**：右詰めゼロパディング  
**文字フィールド**：左詰めスペースパディング（半角スペース）

#### ヘッダーレコード（1件）

| 番号 | 項目名 | 属性 | 桁 | 項目内容 |
|---|---|---|---|---|
| 1 | データ区分 | N(1) | 1 | `1` ヘッダーレコードをセット |
| 2 | 種別コード | N(2) | 2 | `01` 振込入金明細 |
| 3 | コード区分 | N(1) | 1 | 使用文字コードの区分 `0`（JIS） |
| 4 | 作成日 | N(6) | 6 | 操作日 YYMMDD（和暦）、GUIで入力 |
| 5 | 勘定日(自) | N(6) | 6 | 勘定日 YYMMDD（和暦）、GUIで入力 |
| 6 | 勘定日(至) | N(6) | 6 | 勘定日 YYMMDD（和暦）、GUIで入力 |
| 7 | 銀行コード | N(4) | 4 | `0005` 固定 |
| 8 | 銀行名 | C(15) | 15 | `ミツビシユーエフジエイ`（左詰めで残りはスペース） |
| 9 | 支店コード | N(3) | 3 | 照会口座の支店番号、GUIで入力 |
| 10 | 支店名 | C(15) | 15 | 照会口座の支店名（カナ）、左詰めスペース埋め、GUIで入力 |
| 11 | 預金種目 | N(1) | 1 | 科目コード `1`:普通預金 `2`:当座預金、GUIで入力 |
| 12 | 口座番号 | N(7) | 7 | 照会口座の口座番号（右詰め、前は"0"で埋める）、GUIで入力 |
| 13 | 口座名 | C(40) | 40 | 照会口座の口座名（先頭40文字まで）、左詰めスペース埋め、GUIで入力 |
| 14 | ダミー | C(93) | 93 | スペース埋め |

#### データレコード（明細件数分）

| 番号 | 項目名 | 属性 | 桁 | 項目内容 |
|---|---|---|---|---|
| 1 | データ区分 | N(1) | 1 | `2` データレコードをセット |
| 2 | 照会番号 | N(6) | 6 | 明細の番号（右詰め、前は"0"で埋める） |
| 3 | 勘定日 | N(6) | 6 | 取引日 YYMMDD（和暦） ※CSVの値をそのまま使用 |
| 4 | 起算日 | N(6) | 6 | 締後起算日 YYMMDD（和暦） ※CSVの値をそのまま使用 |
| 5 | 金額(1) | N(10) | 10 | 取引金額（右詰め、前は"0"で埋める）。取引金額が10桁以内の場合使用。10桁超の場合はALLゼロを編集 |
| 6 | 他店券金額(1)【うち他店券金額(1)】 | N(10) | 10 | 取引金額中の他店券金額。10桁以内の場合使用。項番13使用時はALLゼロ |
| 7 | 振込依頼人番号【振込依頼人コード】 | C(10) | 10 | 振込依頼人番号（右詰め、前は"0"で埋める） |
| 8 | 振込依頼人名 | C(48) | 48 | 振込依頼人名（左詰めで残りはスペース）。CSVの摘要文字列を使用 |
| 9 | 仕向銀行名 | C(15) | 15 | 振込元の銀行行名（左詰めで残りはスペース）。CSVの銀行名(カナ)を使用 |
| 10 | 仕向店名 | C(15) | 15 | 振込元の預金店名（左詰めで残りはスペース）。CSVの支店名(カナ)を使用 |
| 11 | 取消区分 | N(1) | 1 | `0`:振込 `1`:振込訂正 ※デモ用は`0`固定 |
| 12 | 金額(2) | N(12) | 12 | 取引金額が10桁を超える場合に使用。10桁以下の場合はALLゼロ |
| 13 | 他店券金額(2)【うち他店券金額(2)】 | N(12) | 12 | 取引金額中の他店券金額。10桁超の場合に使用。項番6使用時はALLゼロ |
| 14 | EDI情報 | C(20) | 20 | 依頼人から通知されたEDI情報 ※スペース埋め（デモ用） |
| 15 | ダミー(1) | C(8) | 8 | スペース |
| 16 | カナコメントリスト【ダミー】 | C(6) | 6 | 以下5種類のいずれかのコメント。デモ用はスペース埋め。`ホウシュウ△`/`ハイトウ△△`/`ネンキン△△`/`フリコ＊△`/`フリコ:△△`（△は1バイトのスペース） |
| 17 | ダミー(2) | C(14) | 14 | スペース |

**合計桁数確認**：1+6+6+6+10+10+10+48+15+15+1+12+12+20+8+6+14 = **200バイト**  
※ 実際のレコード長は画像仕様に基づき200バイト。仕様書のレコード長を120バイトから**200バイト**に修正。

#### トレーラーレコード（1件）

| 番号 | 項目名 | 属性 | 桁 | 項目内容 |
|---|---|---|---|---|
| 1 | データ区分 | N(1) | 1 | `8` トレーラーレコードをセット |
| 2 | 振込合計件数 | N(6) | 6 | 取消区分が`0:振込`である明細データの件数（右詰め、前は"0"で埋める） |
| 3 | 振込合計金額 | N(12) | 12 | 取消区分が`0:振込`である明細データの合計金額（右詰め、前は"0"で埋める） |
| 4 | 取消合計件数 | N(6) | 6 | 取消区分が`1:振込訂正`である明細データの件数（右詰め、前は"0"で埋める） |
| 5 | 取消合計金額 | N(12) | 12 | 取消区分が`1:振込訂正`である明細データの合計金額（右詰め、前は"0"で埋める） |
| 6 | ダミー | C(163) | 163 | スペース |

**合計**：1+6+12+6+12+163 = **200バイト**

#### エンドレコード（1件）

| 番号 | 項目名 | 属性 | 桁 | 項目内容 |
|---|---|---|---|---|
| 1 | データ区分 | N(1) | 1 | `9` エンドレコードをセット |
| 2 | 総レコード件数【ダミー】 | N(10) | 10 | エンドデータまで含むデータの総件数（右詰め、前は"0"で埋める） |
| 3 | 伝送口座数 | N(5) | 5 | ヘッダーデータの総件数（右詰め、前は"0"で埋める） |
| 4 | ダミー | C(184) | 184 | スペース |

**合計**：1+10+5+184 = **200バイト**

---

## 4. 非機能要件

- Windows 10以降対応
- 単一実行ファイル（`.exe`）で配布、インストール不要
- 起動時間：3秒以内
- 処理件数：1,000件のCSVを5秒以内に処理
- バリデーションエラーはGUI上でわかりやすく表示

---

## 5. ディレクトリ構成

```
fb-generator/
├── src-tauri/                  # Rustバックエンド
│   ├── src/
│   │   ├── main.rs             # エントリポイント
│   │   ├── lib.rs              # Tauriアプリ設定
│   │   ├── commands/           # Tauriコマンド層
│   │   │   ├── mod.rs
│   │   │   ├── generate.rs     # FB生成コマンド
│   │   │   └── validate.rs     # バリデーションコマンド
│   │   ├── domain/             # ドメイン層
│   │   │   ├── mod.rs
│   │   │   ├── fb_record.rs    # FBレコード構造体
│   │   │   ├── csv_record.rs   # CSV明細構造体（勘定日・起算日・金額・銀行名・支店名・摘要）
│   │   │   └── header_info.rs  # ヘッダー情報構造体
│   │   ├── usecase/            # ユースケース層
│   │   │   ├── mod.rs
│   │   │   └── fb_generator.rs # FB生成ロジック
│   │   ├── infrastructure/     # インフラ層
│   │   │   ├── mod.rs
│   │   │   ├── csv_reader.rs   # CSV読み込み
│   │   │   └── fb_writer.rs    # FBファイル書き込み（Shift-JIS）
│   │   └── error.rs            # エラー型定義
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/                        # フロントエンド（React）
│   ├── components/
│   │   ├── HeaderForm.tsx      # ヘッダー情報入力フォーム
│   │   ├── CsvUploader.tsx     # CSVファイル選択
│   │   ├── PreviewTable.tsx    # CSV内容プレビュー
│   │   ├── ValidationErrors.tsx# エラー表示
│   │   └── GenerateButton.tsx  # 生成実行ボタン
│   ├── hooks/
│   │   ├── useHeaderForm.ts    # ヘッダーフォーム状態管理
│   │   └── useCsvUpload.ts     # CSVアップロード状態管理
│   ├── types/
│   │   ├── header.ts           # ヘッダー情報型定義
│   │   └── csvRecord.ts        # CSV明細型定義
│   ├── App.tsx
│   └── main.tsx
├── tests/                      # テスト
│   └── src-tauri/
│       ├── domain/
│       └── usecase/
└── package.json
```

---

## 6. レイヤード設計

```
フロントエンド（React）
    ↓ invoke（Tauriコマンド）
コマンド層（commands/）   ← Tauri IPC のエントリポイント、引数のデシリアライズのみ
    ↓
ユースケース層（usecase/）  ← ビジネスロジック（FB生成フロー全体）
    ↓
ドメイン層（domain/）      ← データ構造・バリデーションルール・フォーマット変換
    ↓
インフラ層（infrastructure/） ← CSVファイル読み込み・FBファイル書き込み（Shift-JIS）
```

---

## 7. バリデーション仕様

### フロントエンド（即時バリデーション）

| 対象 | ルール |
|---|---|
| 入金日 | YYYY/MM/DD形式、日付として有効であること |
| 銀行コード | 4桁数字 |
| 銀行名 | 半角カタカナ・半角英数字、15文字以内 |
| 支店コード | 3桁数字 |
| 支店名 | 半角カタカナ・半角英数字、15文字以内 |
| 預金種目 | 1または2 |
| 口座番号 | 7桁以内数字 |

### バックエンド（CSV読み込み時）

| 対象 | ルール |
|---|---|
| 金額 | 1以上の整数、12桁以内（10桁超の場合は金額(2)フィールドへ振り分け） |
| 銀行名(カナ) | 半角カタカナ・半角英数字のみ、15文字以内、空欄許容 |
| 支店名(カナ) | 半角カタカナ・半角英数字のみ、15文字以内、空欄許容 |
| 摘要文字列 | 半角カタカナ・半角英数字のみ、48文字以内 |
| EDI | 20文字以内、空欄許容 |
| 振込合計金額 | トレーラーレコードの12桁以内 |
| 行全体 | 空行・カラム数不足の検出 |

バリデーションエラーは行番号・フィールド名・内容をフロントエンドに返す。

---

## 8. エラーハンドリング仕様

- CSVパースエラー：行番号・エラー内容をリスト表示、生成ボタンは非活性
- ファイル書き込みエラー：ダイアログでエラーメッセージ表示
- Shift-JIS変換不可文字：対象行・文字を特定してエラー表示
- **生成成功時**：「FBデータの生成が完了しました」ダイアログを表示して終了
- 全エラーはRust側で`Result<T, AppError>`型で統一管理
- ヘッダー情報の前回値保存は行わない（毎回入力）

---

## 9. UI画面仕様

### メイン画面（1画面構成）

```
┌─────────────────────────────────────────┐
│  FB入金データ生成ツール                    │
├─────────────────────────────────────────┤
│ [1] ヘッダー情報                          │
│   入金日:       [YYYY/MM/DD]              │
│   銀行コード:   [0005]  銀行名:[__________]│
│   支店コード:   [___]   支店名:[__________]│
│   預金種目:     [普通預金▼]               │
│   口座番号:     [_______]                 │
├─────────────────────────────────────────┤
│ [2] 入金明細CSVファイル                   │
│   [ファイルを選択] path/to/file.csv       │
│   ┌─プレビュー（先頭5件）──────────────┐  │
│   │ 銀行CD 銀行名 支店CD 支店名 口座番号 摘要 金額 │  │
│   └─────────────────────────────────┘  │
├─────────────────────────────────────────┤
│ [3] 出力先                               │
│   [保存先を選択] path/to/FB_MMDD.txt     │
├─────────────────────────────────────────┤
│ ⚠ バリデーションエラー表示エリア          │
│                                         │
│              [FBデータを生成]            │
└─────────────────────────────────────────┘
```

---

## 10. 主要クレート・ライブラリ

### Rust（src-tauri）

| クレート | 用途 |
|---|---|
| `tauri` v2 | デスクトップフレームワーク |
| `serde` / `serde_json` | JSON シリアライズ |
| `encoding_rs` | Shift-JIS エンコード |
| `csv` | CSV パース |
| `thiserror` | エラー型定義 |

### フロントエンド

| ライブラリ | 用途 |
|---|---|
| `react` + `typescript` | UI |
| `vite` | ビルドツール |
| `tailwindcss` | スタイリング |
| `react-hook-form` | フォーム管理 |
| `zod` | フロントエンドバリデーション |
| `@tauri-apps/api` | Tauriコマンド呼び出し |

---

## 11. テスト方針

### ユニットテスト（Rust）

- `domain/` 層：各レコード構造体の生成・バリデーション・フォーマット変換
- `usecase/` 層：FB生成ロジック全体（正常系・異常系）
- `infrastructure/` 層：CSVパース処理・Shift-JISエンコード出力

### 統合テスト（Rust）

- テスト用CSVファイルを使ったエンドツーエンドのFBファイル生成
- 生成されたFBファイルのバイト列検証（レコード長120バイト、文字コード確認）
- 境界値テスト（明細0件、最大1000件、金額10桁ちょうど・11桁での金額(1)/(2)切り替え、合計金額12桁上限）

### フロントエンドテスト

- `vitest` によるコンポーネントのユニットテスト
- バリデーションロジックのテスト

---

## 12. 禁止事項・開発規約

1. **GitHubへの更新は明確な指示なしに行わない・提案しないこと**
2. **テストコードを必ず記載すること**（実装コードと同時にテストを作成する）
3. **コーディングを行う際には、一般的なコーディング規則に則り可読性の高いコードを心がけること**
   - Rustは`clippy`・`rustfmt`に準拠
   - TypeScriptは`ESLint` + `Prettier`に準拠
   - 関数・変数名は意図が伝わる命名とする
4. **レイヤード設計を意識すること**
   - 各レイヤーの責務を超えた実装をしない
   - インフラ層の詳細をドメイン層に持ち込まない
   - コマンド層はロジックを持たず、ユースケース層に委譲する
